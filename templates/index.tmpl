<!DOCTYPE html>
<html>
<head>
    <title>Список контейнеров</title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>
<body>
    <header class="top-bar">
        <form action="/containers" method="get" class="search-form">
            <input type="text" name="filter" placeholder="Фильтр">
            <select name="field">
                <option value="type">По типу контейнера</option>
                <option value="id">Серийному номеру</option>
                <option value="cargo">По грузу</option>
                <option value="location">По местоположению</option>
            </select>
            <button class="button button-secondary" type="submit">Поиск</button>
        </form>
    </header>
    <div class="grid grid-cards">
        {{range $key, $value := .}}
            <div class="panel">
                <div class="image-container card-image" style="background-image: url('{{$value.ImageURL}}');"></div>
                <div class="card-content">
                    <h5 class="title">{{$value.Id}}</h5>
                    <p class="info"><b>Тип:</b> {{$value.Type.Name}}</p>
                    <p class="info"><b>Местоположение:</b> {{$value.CurrentLocation}}</p>
                    <p class="info"><b>Груз:</b> {{$value.Cargo.Name}}</p>
                </div>
                <a href="http://localhost:8080/containers/{{$value.Id}}" class="button button-primary">Подробнее</a>
            </div>
        {{end}}
    </div>
</body>
</html>