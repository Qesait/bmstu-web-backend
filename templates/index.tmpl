<!DOCTYPE html>
<html>
<head>
    <title>Список контейнеров</title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>
<body>
    <form action="/containers" method="get">
    <input type="text" name="filter" placeholder="Фильтр">
    <select name = "field">
        <option value = "type"> По типу контейнера </option>
        <option value = "id"> Серийному номеру </option>
        <option value = "cargo"> По грузу </option>
        <option value = "location"> По местоположению </option>
    </select>
    <div class="container-grid">
        {{range $key, $value := .}}
            <div class="card">
                <img class="card-img-top" src="{{$value.ImageURL}}" alt="фото контейнера">
                <h5 class="card-title">№{{$value.Id}}</h5>
                <p class="card-info">Тип: {{$value.Type.Name}}</p>
                <p class="card-info">Местоположение: {{$value.CurrentLocation}}</p>
                <p class="card-info">Груз: {{$value.Cargo.Name}}</p>
                <a href="http://localhost:8080/containers/{{$value.Id}}" class="card-button">Подробнее</a>
            </div>
        {{end}}
    </div>
</body>
</html>